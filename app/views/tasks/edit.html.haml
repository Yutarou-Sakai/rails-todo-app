.container.task_form
    %h2 Edit Task
    - @task.errors.full_messages.each do |message|
        %li= message
    = form_with(model: @task, url: task_path(@task), local: true) do |f|
        = render 'form', f: f